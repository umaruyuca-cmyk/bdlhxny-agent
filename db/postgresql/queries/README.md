# 查询示例

本目录保存只读报表和排障查询，帮助开发者验证网页指标能否从单次运行回算。

[`reporting-examples.sql`](./reporting-examples.sql) 包含：

- 按 Agent 计算有效运行的 p50/p95 时长；
- 检查强制上下文是否被错误省略；
- 从公开工件追溯内部运行。

示例中的 `:batch_id`、`:run_id` 是参数占位符，不是可直接复制到生产执行的值。
本目录不放写入、删除或结构变更语句。
